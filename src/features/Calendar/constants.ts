export const DAYS = Array(7)
  .fill(0)
  .map((_, index) => index);
export const MONTHS = Array(12)
  .fill(0)
  .map((_, index) => index);
export const WEEK_LENGTH = DAYS.length;
export const DAYS_OFF = [6, 0];
export const ORIENTATION_VALUES = ['portrait', 'landscape'];
export const WEEK_START_MODE_VALUES = ['mon', 'sun', 'first'];
export const WIDGET_BREAKPOINTS_VALUES = ['s', 'm', 'l'];
export const AVAILABLE_YEARS_RANGE = 201;
